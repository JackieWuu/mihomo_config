### 使用方法

下载 `.yaml` 文件，根据文件里面的注释添加上订阅链接。将文件上传至 nikki，然后使用如下命令获取 nikki 的 webui 配置，以配合配置文件使用：

```bash
wget -O /etc/config/nikki https://cdn.jsdelivr.net/gh/JackieWuu/mihomo_config@main/nikki/nikki
```

最后启动 Nikki 即可正常运行。
